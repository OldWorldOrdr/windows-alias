{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "process",
            "label": "make-debug",
            "command": "busybox",
            "args": [
                "make",
                "debug"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "isDefault": true,
                "kind": "build"
            }
        }
    ]
}